name: theme_mode_builder
description: A Flutter package for handling theme in an app and also saving it on the device with shared preferences
version: 2.0.0
# author: Yazeed AlKhalaf <yazeedfady@gmail.com>
homepage: https://github.com/YazeedAlKhalaf/theme_mode_builder

environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  shared_preferences: ^2.2.3
  provider: ^6.1.2

  # used just to delete the old hive box :D
  path_provider: ^2.1.3
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
