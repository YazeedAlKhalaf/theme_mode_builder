name: theme_mode_builder
description: A Flutter package for handling theme in an app and also saving it on the device in a hive box.
version: 1.0.2
# author: Yazeed AlKhalaf <yazeedfady@gmail.com>
homepage: https://github.com/YazeedAlKhalaf/theme_mode_builder

environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  hive: ^2.2.3
  hive_flutter: ^1.1.0
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
